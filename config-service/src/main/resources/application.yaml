server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/Harlam7/microservicios
          search-paths: config-data
  application:
    name: config-service